---
name: 2026-05-23-session
description: QMT trader编码修复、止损算法重写、持仓数据同步、回测引擎待建
metadata:
  type: project
---

## 完成事项

### QMT trader 编码与可靠性
- trader.py 彻底清理为纯ASCII + `#coding:latin-1` 编码声明
- 修复 `__file__` 不存在的问题，改用 `os.getcwd()`
- 修复中文路径问题：Streamlit 写文件到 `D:\` 根目录作为回退
- 修复 `g.holdings` key 格式匹配（同时检查带/不带后缀）
- 修复 `g.acct/g.acct_type` 未定义问题，改用 QMT 内置 `account, "stock"`
- 修复 `get_script_dir()` 中文路径被ASCII清理删除的bug，用unicode转义恢复
- 所有日志改为英文标签，QMT控制台不再乱码

### 止损算法重写
- 过滤 >15% 单日波动（分拆/除权/数据错误）
- Winsorize 3σ 极端值后计算年化波动率
- 阈值改为 `ann_vol * 100`（1σ年化），区间 [10%, 25%]
- Peak：检测到分拆事件后用事件后数据 + top-3中位数
- QMT 导出时 price 从 `get_trade_detail_data("position")` 的 `m_dMarketValue/shares` 计算，不依赖 `get_full_tick`
- 成本价从 `m_dOpenPrice` 读取

### 持仓数据同步
- `qmt_holdings.json` 实际写到 `D:\长城策略交易系统\bin.x64\`（QMT cwd）
- Streamlit `load_actual_holdings()` 优先读 `bin.x64` 路径，回退 `python` 路径
- 确认成本价、市值、盈亏与用户实际持仓一致

### 止损触发记录
- QMT 初始化时创建空 `stoploss_log.json`
- 触发止损时追加事件（时间/代码/价格/回撤）
- Streamlit 策略信号Tab底部展示"止损触发记录"表

### 策略改进
- 改进1: 统一评分公式 — `_score_etf` 改为 `@classmethod`，SCORE_WEIGHTS 可配置
- 改进5: 全周期 trailing profit 止盈（扩张40%/复苏30%/滞胀20%/衰退15%）
- 改进8: QMT指令显示交易成本

### 待完成
- 回测引擎：需要建立统一的回测引擎作为唯一依据
- 回测对比不同 SCORE_WEIGHTS 方案
- `scripts/_backtest_weights.py` 已写好但未跑完（缓存问题需清缓存）

## QMT 策略文件路径
- 源文件: `qmt/trader.py`
- QMT交付: `D:\长城策略交易系统\python\_trader_code.txt`
- 用户手动粘贴到 QMT 策略编辑器

## 关键文件
- `src/agent/engine.py`: PortfolioAgent + MacroAgent
- `app.py`: Streamlit UI
- `qmt/trader.py`: QMT策略
- `CLAUDE.md`: QMT代码编写铁律已更新
